import React, { Component } from 'react';

class Palindromes extends Component {
  constructor(props){
    super(props);
    console.log(props)
  }
  render() {
    return (
      <div> Palindromes </div>
    );
  }
}

export default Palindromes;
